# Changesets

For maintainers only. The published release is the **Dimensions Analytics MCP server** (`apps/mcp` → npm `@digital-science-dsl/dimensions-analytics-mcp`).

```bash
pnpm run changeset
```

The workspace contains only `apps/mcp`; there are no separate published library packages.
