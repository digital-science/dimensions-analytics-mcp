---
"@digital-science-dsl/dimensions-analytics-mcp": patch
---

Add the `mcpName` ownership marker and npm keywords so the package can back a record in the official MCP Registry under `ai.dimensions/analytics-mcp`.
